import { FontTypeEnum, LanguageEnum, Settings, ThemeEnum } from "@/types/settings";

export const initialSettings: Settings = {
    fontType: FontTypeEnum.LORA,
    language: LanguageEnum.ENGLISH,
    theme: ThemeEnum.LIGHT,
}