import PlayIcon from "@/components/Icons/PlayIcon"
import { PlayButtonContainer } from "./PlayButtonStyled"

const PlayButton = () => {
    return (
        <PlayButtonContainer href='/categories'>
            <PlayIcon />
        </PlayButtonContainer>
    )
}
export default PlayButton